[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "course-common"
version = "0.1.0"
description = "Small, teacher-owned contracts shared by the AI Agents course."
requires-python = ">=3.12,<3.13"
dependencies = []
